<ul class="products__list">
  {{#each products}}
    <li class="product">
        <h2 class="product__title">{{name}}</h2>
        <p class="product__price">${{price}}</p>
        <p class="product__describtion">{{description}}</p>
    </li>
  {{/each}}
</ul>